name: GitHub Runner Probe

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/context-core-ci.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Verify runner can start
        run: echo "AdaAI GitHub runner is available"
